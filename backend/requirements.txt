# --- Web framework -----------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Data --------------------------------------------------------------------
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
pgvector==0.3.6

# --- Validation & config -----------------------------------------------------
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0

# --- Auth --------------------------------------------------------------------
PyJWT==2.10.1
bcrypt==4.2.1

# --- Imaging & face recognition ----------------------------------------------
# onnxruntime and opencv-python ship prebuilt wheels for Windows, macOS and
# Linux, so `pip install` needs no compiler. This is why the ArcFace models are
# run directly rather than through the `insightface` package, which does.
Pillow==11.0.0
numpy==2.2.1
opencv-python-headless==4.10.0.84
onnxruntime==1.20.1
qrcode[pil]==8.0

# --- Tests -------------------------------------------------------------------
pytest==8.3.4
httpx==0.28.1
